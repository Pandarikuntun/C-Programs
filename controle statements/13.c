/*in the following code. add braces so that the printf statements
 make sense and there is no dangling else problem do proper indentation*/
 #include<stdio.h>
 main(){
 	int a,b,c;
 	printf("enetr the a b c values :");
 	scanf("%d%d%d",&a,&b,&c);
 	if(a==10){
 		if(b==20){
 			if(c==30)
 				printf("a is 10\nb is 20\nc is 30");
		 	else
		 	printf("a is 10\nbis not 20");
			 }
		 }
		else
		 	
		 	printf("a is not 10");
	 	
	 }
