//using if else statement checking of the person is eligble for voting or not
#include<stdio.h>
main(){
	int x;
	printf("enter the person age :");
	scanf("%d",&x);
	if(x<18)
	printf("not eligible for voting");
	else
	printf("eligible for votinig");
} 
